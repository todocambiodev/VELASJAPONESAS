from setuptools import setup, find_packages

setup(
    name="velasJaponeas",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Lista de dependencias si las tiene, ej: 'requests>=2.25.1'
    ],
)
